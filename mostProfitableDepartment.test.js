describe('the most profitable department', function(){
  it('should find the department that is most profitable', function(){
    assert.equal('outdoor', mostProfitableDepartment(salesData));
  });
  it('should find the department that is most profitable', function(){
    assert.equal('outdoor', mostProfitableDepartment(salesData));
  });
});
